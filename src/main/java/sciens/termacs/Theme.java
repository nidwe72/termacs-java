package sciens.termacs;

/** Built-in theme (ordinal matches TmTheme in the C ABI).
 *  PHOSPHOR_HARMONY is the green/amber/gray "green-screen" palette (§5.6). */
public enum Theme { DARK, LIGHT, PHOSPHOR_HARMONY }
